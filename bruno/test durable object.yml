info:
  name: test durable object
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8787/do/test-2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: example
    description: test-2 was called a bunch of times, but only state is encapsulated
    request:
      url: http://localhost:8787/do/test-2
      method: GET
    response:
      statusText: OK
      headers:
        - name: transfer-encoding
          value: chunked
        - name: content-type
          value: application/json
      body:
        type: json
        data: |-
          {
            "count": 11
          }
